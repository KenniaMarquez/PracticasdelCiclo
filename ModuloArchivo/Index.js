const chosenCulturas = require('Culturas');

const chosenCultura = Culturas.getRandomColor();
console.log(`You should use ${chosenCultura.name} on your website. It's HTML code is ${chosenCultura.code}`);
